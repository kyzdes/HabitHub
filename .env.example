# Database Configuration
# Get your PostgreSQL connection string from your database provider
# For local development, you can use a local PostgreSQL instance
# For production, recommended: Neon, Supabase, or your own PostgreSQL server
DATABASE_URL=postgresql://username:password@host:5432/database_name

# Session Secret
# Generate a random string for production: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-secure-random-string-in-production

# Node Environment
NODE_ENV=production

# Server Port
PORT=5000

# Application URL (for production)
APP_URL=https://yourdomain.com

# Optional: Google OAuth (if you want to add Google login later)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# GOOGLE_CALLBACK_URL=https://yourdomain.com/api/auth/google/callback
